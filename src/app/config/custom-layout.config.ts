import {LayoutConfig} from "@spartacus/storefront";

export const customLayoutConfig: LayoutConfig = {
  layoutSlots: {
    LandingPage2Template: {
      lg: {
        slots: ['Section3', 'Section2A', 'Section2B', 'Section2C', 'Section1', 'Section4']
      },
     slots: ['Section1']
    }
  }
}
